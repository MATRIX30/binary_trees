#include "binary_trees.h"
/**
 * heap_extract - function to extract root node of a Max Binary Heap
 * @root: double pointer to the root node of heap
 * Return: value stored in the root node
*/
int heap_extract(heap_t **root)
{
	return (root);
}
